version: '3.8'

services:
  app:
    build: .
    container_name: it-admin-portal
    restart: always
    ports:
      - "3001:3001"                # 🟢 เปิด Port 3001 (เข้าผ่าน http://dev.biccorp.com:3001 ไม่ได้ ต้องผ่าน Tunnel หรือแก้ Nginx)
    environment:
      - PORT=3001
      - AD_URL=ldap://samba-ad
      - AD_BASE_DN=DC=ad,DC=biccorp,DC=com
      - AD_USER=Administrator@ad.biccorp.com
      - AD_PASS=BicCorp@AD2026!
    volumes:
      - .:/app
      - /app/node_modules
    networks:
      - app-network

networks:
  app-network:
    external: true
    name: it-log-app_app-network   # ✅ เชื่อม Network เดียวกันกับ Samba