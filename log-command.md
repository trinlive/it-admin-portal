นี่คือรวมคำสั่งสำหรับดู Log ของ App นี้ เลือกใช้ตามสถานการณ์ได้เลยครับ:

1. ดู Log แบบ Real-time (แนะนำ 🔥)

คำสั่งนี้จะแสดง Log ล่าสุดและจะ "ค้างหน้าจอไว้" เพื่อรอแสดง Log ใหม่ๆ ที่กำลังจะเกิดขึ้น (เหมือน tail -f) เหมาะสำหรับเปิดทิ้งไว้ตอนกำลังกดทดสอบหน้าเว็บครับ

Bash
docker logs -f it-admin-portal
(กด Ctrl + C เพื่อออกจากหน้านี้)

2. ดู Log ย้อนหลัง 100 บรรทัดล่าสุด (แบบ Real-time)

ถ้า Log เก่าๆ มันเยอะเกินไปจนตาลาย ให้ใช้คำสั่งนี้เพื่อตัดมาดูแค่ช่วงท้ายๆ ครับ

Bash
docker logs --tail 100 -f it-admin-portal
3. ดู Log ทั้งหมดตั้งแต่อดีตจนถึงปัจจุบัน

ถ้าอยากไล่ดูประวัติการทำงานทั้งหมด (ระวังหน้าจอไหลยาวมาก)

Bash
docker logs it-admin-portal
4. ค้นหาคำ error เฉพาะเจาะจง (Grep)

ถ้าอยากหาว่ามี Error อะไรเกิดขึ้นบ้าง โดยไม่ต้องนั่งไล่อ่านเอง

Bash
docker logs it-admin-portal | grep "Error"
หรือหาคำว่า "Connection" เพื่อดูการเชื่อมต่อ

Bash
docker logs it-admin-portal | grep "Connection"
💡 ข้อแนะนำ: เนื่องจากคุณกำลังแก้ปัญหาเรื่องการเชื่อมต่อกับ Samba AD แนะนำให้เปิด 2 Terminal คู่กันครับ:

Terminal 1: docker logs -f it-admin-portal (ดูฝั่ง Node.js App ว่าส่งค่าอะไรไป)

Terminal 2: docker logs -f samba-ad (ดูฝั่ง Samba ว่าได้รับค่าไหม)