<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit User - IT Admin Portal</title>
    
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Sarabun:wght@300;400;500;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    <style>
        body { font-family: 'Sarabun', sans-serif; }
    </style>
</head>
<body class="bg-gray-50 text-gray-800 min-h-screen flex flex-col">

    <nav class="bg-slate-900 text-white shadow-lg">
        <div class="container mx-auto px-4 py-3">
            <a href="/" class="text-xl font-bold flex items-center gap-2 hover:text-gray-200 transition w-fit">
                <i class="fa-solid fa-arrow-left"></i> กลับหน้าหลัก
            </a>
        </div>
    </nav>

    <div class="container mx-auto px-4 py-10 flex-grow flex items-center justify-center">
        <div class="w-full max-w-2xl">
            
            <div class="bg-white rounded-xl shadow-lg border border-gray-200 overflow-hidden">
                <div class="bg-gradient-to-r from-blue-600 to-indigo-600 px-8 py-5 text-white">
                    <h4 class="text-xl font-bold flex items-center gap-2">
                        <i class="fa-solid fa-user-pen"></i> แก้ไขข้อมูลผู้ใช้ (Edit User)
                    </h4>
                    <p class="text-blue-100 text-sm mt-1">อัปเดตข้อมูล: <%= user.cn %></p>
                </div>

                <div class="p-8">
                    <form action="/users/update" method="POST" class="space-y-6">
                        <input type="hidden" name="dn" value="<%= user.dn %>">

                        <div>
                            <label class="block text-sm font-medium text-gray-700 mb-1">Username</label>
                            <div class="relative">
                                <span class="absolute inset-y-0 left-0 pl-3 flex items-center text-gray-400"><i class="fa-solid fa-lock"></i></span>
                                <input type="text" value="<%= user.sAMAccountName %>" disabled
                                    class="w-full pl-10 pr-4 py-2 border border-gray-200 bg-gray-100 text-gray-500 rounded-lg cursor-not-allowed">
                            </div>
                            <p class="text-xs text-gray-400 mt-1 ml-1">Username ไม่สามารถแก้ไขได้</p>
                        </div>
                        
                        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                            <div>
                                <label class="block text-sm font-medium text-gray-700 mb-1">ชื่อจริง (First Name)</label>
                                <input type="text" name="firstName" value="<%= user.givenName || '' %>" required 
                                    class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500 outline-none transition">
                            </div>
                            <div>
                                <label class="block text-sm font-medium text-gray-700 mb-1">นามสกุล (Last Name)</label>
                                <input type="text" name="lastName" value="<%= user.sn || '' %>" required 
                                    class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500 outline-none transition">
                            </div>
                        </div>

                        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                            <div>
                                <label class="block text-sm font-medium text-gray-700 mb-1">อีเมล (Email)</label>
                                <div class="relative">
                                    <span class="absolute inset-y-0 left-0 pl-3 flex items-center text-gray-400"><i class="fa-regular fa-envelope"></i></span>
                                    <input type="email" name="email" value="<%= user.mail || '' %>"
                                        class="w-full pl-10 pr-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500 outline-none transition">
                                </div>
                            </div>
                            <div>
                                <label class="block text-sm font-medium text-gray-700 mb-1">แผนก (Department)</label>
                                <div class="relative">
                                    <span class="absolute inset-y-0 left-0 pl-3 flex items-center text-gray-400"><i class="fa-solid fa-building"></i></span>
                                    <input type="text" name="department" value="<%= user.department || '' %>"
                                        class="w-full pl-10 pr-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500 outline-none transition">
                                </div>
                            </div>
                        </div>

                        <div class="pt-4 flex items-center justify-end gap-3 border-t border-gray-100 mt-6">
                            <a href="/" class="px-5 py-2.5 rounded-lg text-gray-600 bg-white border border-gray-300 hover:bg-gray-50 font-medium transition duration-200">
                                ยกเลิก
                            </a>
                            <button type="submit" class="px-6 py-2.5 rounded-lg text-white bg-blue-600 hover:bg-blue-700 font-medium shadow-md hover:shadow-lg transition duration-200">
                                <i class="fa-solid fa-save mr-2"></i>บันทึกการแก้ไข
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</body>
</html>