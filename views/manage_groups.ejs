<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Manage Groups - IT Admin Portal</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Sarabun:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <style> 
        body { font-family: 'Sarabun', sans-serif; background-color: #f8fafc; } 
        /* Custom Scrollbar for Dropdown if needed */
        select::-webkit-scrollbar { width: 8px; }
        select::-webkit-scrollbar-track { background: #f1f1f1; }
        select::-webkit-scrollbar-thumb { background: #cbd5e1; border-radius: 4px; }
        select::-webkit-scrollbar-thumb:hover { background: #94a3b8; }
    </style>
</head>
<body class="text-slate-800 min-h-screen flex flex-col">

    <nav class="bg-slate-900 text-white shadow-xl sticky top-0 z-50 border-b border-slate-800">
        <div class="container mx-auto px-4 py-3 flex justify-between items-center">
            <a href="/" class="text-lg font-bold flex items-center gap-2 hover:text-indigo-300 transition w-fit group">
                <div class="bg-slate-800 p-1.5 rounded-lg group-hover:bg-slate-700 transition">
                    <i class="fa-solid fa-arrow-left"></i>
                </div>
                <span>กลับหน้าหลัก</span>
            </a>
            <div class="text-sm text-slate-400 hidden sm:block">
                <i class="fa-solid fa-server mr-1"></i> Active Directory
            </div>
        </div>
    </nav>

    <div class="container mx-auto px-4 py-10 max-w-6xl">
        
        <div class="flex flex-col md:flex-row md:items-center gap-6 mb-10">
            <div class="w-20 h-20 rounded-2xl bg-white border border-slate-100 flex items-center justify-center shadow-lg shadow-indigo-500/10 shrink-0">
                <i class="fa-solid fa-users-gear text-4xl text-indigo-600 bg-gradient-to-br from-indigo-500 to-purple-600 bg-clip-text text-transparent"></i>
            </div>
            <div>
                <h1 class="text-3xl font-bold text-slate-800 tracking-tight">จัดการกลุ่ม (Group Management)</h1>
                <div class="flex items-center gap-2 mt-2 text-slate-500 font-medium">
                    <i class="fa-regular fa-user text-indigo-500"></i>
                    <span>จัดการสิทธิ์ของ:</span>
                    <span class="text-indigo-700 bg-indigo-50 px-2.5 py-0.5 rounded-md border border-indigo-100 text-sm font-bold">
                        <%= user.sAMAccountName %>
                    </span>
                    <span class="text-sm text-slate-400 hidden sm:inline">(<%= user.cn %>)</span>
                </div>
            </div>
        </div>

        <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
            
            <div class="lg:col-span-2 space-y-6">
                <div class="bg-white rounded-2xl shadow-sm border border-slate-200 overflow-hidden">
                    <div class="px-6 py-5 border-b border-slate-100 flex justify-between items-center bg-slate-50/50 backdrop-blur-sm">
                        <h3 class="font-bold text-slate-700 flex items-center gap-2 text-lg">
                            <span class="bg-indigo-100 text-indigo-600 w-8 h-8 flex items-center justify-center rounded-lg shadow-sm">
                                <i class="fa-solid fa-layer-group text-sm"></i>
                            </span>
                            กลุ่มปัจจุบัน <span class="bg-slate-200 text-slate-600 text-xs px-2 py-0.5 rounded-full ml-1"><%= userGroups.length %></span>
                        </h3>
                    </div>
                    
                    <div class="divide-y divide-slate-100 max-h-[600px] overflow-y-auto">
                        <% if (userGroups.length > 0) { %>
                            <% userGroups.forEach(groupDN => { 
                                // Clean Group Name
                                const groupName = groupDN.match(/^CN=([^,]+)/) ? groupDN.match(/^CN=([^,]+)/)[1] : groupDN;
                                const isPrimary = groupName === 'Domain Users';
                            %>
                                <div class="px-6 py-4 flex flex-wrap sm:flex-nowrap justify-between items-center hover:bg-slate-50 transition group duration-200">
                                    <div class="flex items-center gap-4 min-w-0">
                                        <div class="w-10 h-10 rounded-xl flex items-center justify-center text-lg shadow-sm shrink-0
                                            <%= isPrimary ? 'bg-amber-50 text-amber-600' : 'bg-indigo-50 text-indigo-600 group-hover:bg-indigo-100 group-hover:text-indigo-700 transition-colors' %>">
                                            <i class="<%= isPrimary ? 'fa-solid fa-shield-halved' : 'fa-solid fa-user-group' %>"></i>
                                        </div>
                                        <div class="min-w-0">
                                            <p class="font-bold text-slate-700 text-base truncate pr-4"><%= groupName %></p>
                                            <p class="text-xs text-slate-400 truncate max-w-md font-mono" title="<%= groupDN %>">
                                                <%= groupDN.length > 50 ? groupDN.substring(0, 50) + '...' : groupDN %>
                                            </p>
                                        </div>
                                    </div>

                                    <div class="mt-3 sm:mt-0 flex items-center gap-3 shrink-0 ml-auto sm:ml-0">
                                        <% if (!isPrimary) { %>
                                            <form action="/groups/remove" method="POST" onsubmit="return confirmRemove(event, '<%= groupName %>')">
                                                <input type="hidden" name="userDN" value="<%= user.dn %>">
                                                <input type="hidden" name="groupDN" value="<%= groupDN %>">
                                                <button type="submit" class="text-slate-400 hover:text-red-600 hover:bg-red-50 w-9 h-9 flex items-center justify-center rounded-lg transition-all duration-200 border border-transparent hover:border-red-100" title="นำออกจากกลุ่ม">
                                                    <i class="fa-solid fa-trash-can"></i>
                                                </button>
                                            </form>
                                        <% } else { %>
                                            <span class="text-xs font-bold text-amber-600 bg-amber-50 border border-amber-100 px-3 py-1 rounded-full flex items-center gap-1 shadow-sm">
                                                <i class="fa-solid fa-lock text-[10px]"></i> Primary
                                            </span>
                                        <% } %>
                                    </div>
                                </div>
                            <% }); %>
                        <% } else { %>
                            <div class="py-16 text-center text-slate-400 flex flex-col items-center">
                                <div class="bg-slate-50 p-6 rounded-full mb-4">
                                    <i class="fa-solid fa-folder-open text-4xl opacity-40"></i>
                                </div>
                                <p class="text-lg font-medium text-slate-500">ว่างเปล่า</p>
                                <p class="text-sm">User นี้ยังไม่ได้สังกัดกลุ่มใด</p>
                            </div>
                        <% } %>
                    </div>
                </div>
            </div>

            <div class="lg:col-span-1">
                <div class="bg-white rounded-2xl shadow-xl border border-indigo-100 overflow-hidden sticky top-24 transform transition-all hover:shadow-2xl hover:shadow-indigo-200/50 duration-300">
                    
                    <div class="bg-gradient-to-r from-indigo-600 to-violet-600 px-6 py-5 text-white relative overflow-hidden">
                        <div class="absolute -right-6 -top-6 w-24 h-24 bg-white opacity-10 rounded-full blur-2xl"></div>
                        <h3 class="font-bold flex items-center gap-2 text-lg relative z-10">
                            <i class="fa-solid fa-plus-circle"></i> เพิ่มเข้ากลุ่มใหม่
                        </h3>
                        <p class="text-indigo-100 text-xs mt-1 relative z-10 opacity-90">เลือกกลุ่มที่ต้องการให้สิทธิ์</p>
                    </div>

                    <div class="p-6">
                        <form action="/groups/add" method="POST">
                            <input type="hidden" name="userDN" value="<%= user.dn %>">
                            
                            <div class="mb-5">
                                <label class="block text-sm font-bold text-slate-700 mb-2 ml-1">
                                    เลือกกลุ่ม (Select Group) <span class="text-red-500">*</span>
                                </label>
                                
                                <div class="relative group">
                                    <select name="groupDN" required 
                                        class="w-full appearance-none pl-4 pr-10 py-3.5 border border-slate-300 rounded-xl focus:ring-4 focus:ring-indigo-100 focus:border-indigo-500 outline-none text-sm bg-white text-slate-700 font-medium transition-all shadow-sm group-hover:border-indigo-300 cursor-pointer">
                                        <option value="" disabled selected>-- กรุณาเลือกกลุ่ม --</option>
                                        <% allGroups.forEach(g => { 
                                            const isMember = userGroups.includes(g.dn);
                                        %>
                                            <option value="<%= g.dn %>" <%= isMember ? 'disabled' : '' %> class="py-2">
                                                <%= g.cn %> <%= isMember ? '✅ (มีอยู่แล้ว)' : '' %>
                                            </option>
                                        <% }); %>
                                    </select>
                                    
                                    <div class="absolute inset-y-0 right-0 pr-4 flex items-center pointer-events-none text-slate-400 group-hover:text-indigo-500 transition-colors">
                                        <i class="fa-solid fa-chevron-down text-xs"></i>
                                    </div>
                                </div>
                                
                                <p class="text-xs text-slate-400 mt-2.5 ml-1 flex items-start gap-1.5 leading-relaxed">
                                    <i class="fa-solid fa-circle-info mt-0.5 text-indigo-400"></i> 
                                    <span>ระบบจะแสดงเฉพาะกลุ่มที่ User <br>ยังไม่ได้เป็นสมาชิกเท่านั้น</span>
                                </p>
                            </div>

                            <button type="submit" class="w-full py-3.5 bg-indigo-600 hover:bg-indigo-700 text-white rounded-xl font-bold shadow-lg shadow-indigo-500/30 hover:shadow-indigo-600/40 hover:-translate-y-0.5 active:translate-y-0 transition-all duration-200 flex items-center justify-center gap-2 group">
                                <span class="group-hover:scale-110 transition-transform duration-200"><i class="fa-solid fa-user-plus"></i></span>
                                <span>บันทึกการเพิ่มกลุ่ม</span>
                            </button>
                        </form>
                    </div>
                </div>
                
                <div class="mt-6 bg-slate-50 border border-slate-200 rounded-xl p-5 text-sm text-slate-500">
                    <h4 class="font-bold text-slate-700 mb-2 text-xs uppercase tracking-wide">
                        <i class="fa-regular fa-lightbulb text-yellow-500 mr-1"></i> คำแนะนำ
                    </h4>
                    <ul class="space-y-2 list-disc pl-4 text-xs leading-relaxed">
                        <li>กลุ่ม <b>Domain Users</b> เป็นกลุ่มหลักของระบบ ไม่สามารถนำออกได้</li>
                        <li>การเพิ่มกลุ่มอาจใช้เวลา 1-2 นาทีกว่าสิทธิ์จะเริ่มทำงานในเครื่องลูกข่าย (ขึ้นอยู่กับ AD Replication)</li>
                    </ul>
                </div>
            </div>

        </div>
    </div>

    <script>
        function confirmRemove(e, groupName) {
            e.preventDefault();
            const form = e.target;
            
            Swal.fire({
                title: 'ยืนยันการนำออก?',
                html: `ต้องการนำ User ออกจากกลุ่ม <br><span class="text-indigo-600 font-bold text-lg block mt-2">${groupName}</span>`,
                icon: 'warning',
                showCancelButton: true,
                focusCancel: true,
                confirmButtonColor: '#ef4444',
                cancelButtonColor: '#ffffff',
                confirmButtonText: '<i class="fa-solid fa-trash-can mr-2"></i>ใช่, นำออกเลย',
                cancelButtonText: '<span class="text-slate-600">ยกเลิก</span>',
                buttonsStyling: false,
                customClass: {
                    popup: 'rounded-2xl shadow-xl font-sans',
                    confirmButton: 'bg-red-500 hover:bg-red-600 text-white font-medium py-2.5 px-6 rounded-lg shadow-lg hover:shadow-red-500/30 transition-all mr-2',
                    cancelButton: 'bg-white hover:bg-slate-50 text-slate-700 font-medium py-2.5 px-6 rounded-lg border border-slate-200 hover:border-slate-300 transition-all'
                }
            }).then((result) => {
                if (result.isConfirmed) {
                    // Show Loading Animation
                    Swal.fire({
                        title: 'กำลังดำเนินการ...',
                        html: 'กรุณารอสักครู่ ระบบกำลังอัปเดต AD',
                        allowOutsideClick: false,
                        didOpen: () => { Swal.showLoading(); },
                        customClass: { popup: 'rounded-2xl shadow-xl font-sans' }
                    });
                    form.submit();
                }
            });
            return false;
        }
    </script>
</body>
</html>