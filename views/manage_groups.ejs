<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Group Management - IT Admin Portal</title>
    
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Sarabun:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

    <style>
        body { font-family: 'Sarabun', sans-serif; }
    </style>
</head>
<body class="bg-slate-50 text-slate-800 min-h-screen flex flex-col">

    <nav class="bg-slate-900 text-white shadow-lg sticky top-0 z-50">
        <div class="container mx-auto px-4 py-3 flex justify-between items-center">
            <a href="/" class="text-lg font-bold flex items-center gap-2 hover:text-slate-200 transition">
                <i class="fa-solid fa-arrow-left"></i> กลับหน้าหลัก
            </a>
            <div class="text-sm opacity-80">
                <i class="fa-solid fa-server mr-1"></i> Active Directory Manager
            </div>
        </div>
    </nav>

    <div class="container mx-auto px-4 py-10 flex-grow">
        
        <div class="flex flex-col md:flex-row items-start md:items-center gap-4 mb-8">
            <div class="bg-white p-4 rounded-2xl shadow-sm border border-slate-100 flex items-center justify-center text-indigo-600">
                <i class="fa-solid fa-users-gear text-3xl"></i>
            </div>
            <div>
                <h1 class="text-3xl font-bold text-slate-800">จัดการกลุ่ม (Group Management)</h1>
                <p class="text-slate-500 mt-1 flex items-center gap-2">
                    <i class="fa-regular fa-user"></i> จัดการสิทธิ์ของ: 
                    <span class="bg-indigo-100 text-indigo-700 px-3 py-0.5 rounded-full font-bold text-sm">
                        <%= user.sAMAccountName %>
                    </span>
                    <span class="text-xs text-slate-400">(<%= user.cn %>)</span>
                </p>
            </div>
        </div>

        <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
            
            <div class="lg:col-span-2 space-y-6">
                <div class="bg-white rounded-2xl shadow-sm border border-slate-200 p-6 min-h-[400px]">
                    
                    <div class="flex items-center justify-between mb-6">
                        <div class="flex items-center gap-3">
                            <div class="bg-indigo-100 text-indigo-600 p-2 rounded-lg">
                                <i class="fa-solid fa-layer-group"></i>
                            </div>
                            <h3 class="font-bold text-slate-800 text-lg">กลุ่มปัจจุบัน</h3>
                            <span class="bg-slate-100 text-slate-600 px-2.5 py-0.5 rounded-full text-xs font-bold">
                                <%= typeof userGroups !== 'undefined' ? userGroups.length : 0 %>
                            </span>
                        </div>
                    </div>

                    <% if (typeof userGroups !== 'undefined' && userGroups.length > 0) { %>
                        <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                            <% userGroups.forEach(function(group) { %>
                                <div class="flex items-center justify-between p-4 rounded-xl border border-slate-100 bg-slate-50/50 hover:border-indigo-200 hover:bg-indigo-50 transition group-card">
                                    <div class="flex items-center gap-3 overflow-hidden">
                                        <div class="w-10 h-10 rounded-full bg-white border border-slate-200 flex items-center justify-center text-indigo-500 shadow-sm shrink-0">
                                            <i class="fa-solid fa-users text-sm"></i>
                                        </div>
                                        <div class="min-w-0">
                                            <p class="font-bold text-slate-700 text-sm truncate" title="<%= group.cn %>">
                                                <%= group.cn %>
                                            </p>
                                            <p class="text-[10px] text-slate-400 truncate max-w-[150px]" title="<%= group.dn %>">
                                                <%= group.dn %>
                                            </p>
                                        </div>
                                    </div>

                                    <% if (group.cn === 'Domain Users') { %>
                                        <span class="text-xs text-slate-400 bg-slate-100 px-2 py-1 rounded" title="กลุ่มบังคับของระบบ">
                                            <i class="fa-solid fa-lock"></i>
                                        </span>
                                    <% } else { %>
                                        <form action="/groups/remove" method="POST" id="removeForm-<%= group.cn %>">
                                            <input type="hidden" name="userDN" value="<%= user.dn %>">
                                            <input type="hidden" name="groupDN" value="<%= group.dn %>">
                                            
                                            <button type="button" 
                                                onclick="confirmRemoveGroup('<%= group.cn %>', 'removeForm-<%= group.cn %>')"
                                                class="w-8 h-8 flex items-center justify-center rounded-lg text-slate-400 hover:text-red-600 hover:bg-red-100 transition" 
                                                title="นำออกจากกลุ่ม">
                                                <i class="fa-solid fa-trash-can"></i>
                                            </button>
                                        </form>
                                    <% } %>
                                </div>
                            <% }); %>
                        </div>
                    <% } else { %>
                        <div class="flex flex-col items-center justify-center h-64 text-slate-300">
                            <i class="fa-regular fa-folder-open text-4xl mb-3 opacity-50"></i>
                            <p class="text-sm font-medium">ว่างเปล่า</p>
                            <p class="text-xs mt-1">User นี้ยังไม่ได้สังกัดกลุ่มใด (หรือเป็นกลุ่ม Primary)</p>
                        </div>
                    <% } %>

                </div>
            </div>

            <div class="lg:col-span-1">
                <div class="bg-white rounded-2xl shadow-md border border-indigo-100 overflow-hidden sticky top-24">
                    <div class="bg-gradient-to-r from-indigo-600 to-violet-600 px-6 py-4 text-white">
                        <h4 class="font-bold flex items-center gap-2">
                            <i class="fa-solid fa-circle-plus"></i> เพิ่มเข้ากลุ่มใหม่
                        </h4>
                        <p class="text-indigo-100 text-xs mt-1">เลือกกลุ่มที่ต้องการให้สิทธิ์</p>
                    </div>

                    <div class="p-6">
                        <form action="/groups/add" method="POST" class="space-y-4">
                            <input type="hidden" name="userDN" value="<%= user.dn %>">
                            
                            <div>
                                <label class="block text-sm font-bold text-slate-700 mb-2">เลือกกลุ่ม (Select Group) <span class="text-red-500">*</span></label>
                                <div class="relative">
                                    <select name="groupDN" required class="w-full pl-3 pr-10 py-2.5 border border-slate-300 rounded-xl focus:ring-2 focus:ring-indigo-500 outline-none transition bg-white text-sm">
                                        <option value="">-- กรุณาเลือกกลุ่ม --</option>
                                        
                                        <% if (typeof allGroups !== 'undefined' && allGroups.length > 0) { %>
                                            <% allGroups.forEach(function(g) { %>
                                                <% 
                                                    const isMember = userGroups && userGroups.some(ug => ug.dn === g.dn || ug.cn === g.cn);
                                                    if (!isMember) { 
                                                %>
                                                    <option value="<%= g.dn %>"><%= g.cn %></option>
                                                <% } %>
                                            <% }); %>
                                        <% } %>
                                    </select>
                                    <div class="absolute inset-y-0 right-0 pr-3 flex items-center pointer-events-none text-slate-400">
                                        <i class="fa-solid fa-chevron-down text-xs"></i>
                                    </div>
                                </div>
                                <p class="text-xs text-slate-500 mt-2">
                                    <i class="fa-solid fa-circle-info text-indigo-500 mr-1"></i> ระบบจะแสดงเฉพาะกลุ่มที่ User ยังไม่ได้เป็นสมาชิก
                                </p>
                            </div>

                            <button type="submit" class="w-full py-2.5 rounded-xl text-white bg-indigo-600 hover:bg-indigo-700 font-bold shadow-md hover:shadow-lg transition flex items-center justify-center gap-2">
                                <i class="fa-solid fa-user-plus"></i> บันทึกการเพิ่มกลุ่ม
                            </button>
                        </form>
                    </div>
                    
                    <div class="bg-indigo-50/50 p-4 border-t border-indigo-100">
                        <div class="flex items-start gap-2">
                            <i class="fa-regular fa-lightbulb text-amber-500 mt-0.5"></i>
                            <div class="text-xs text-slate-600 space-y-1">
                                <p class="font-bold text-slate-700">คำแนะนำ</p>
                                <ul class="list-disc pl-3 space-y-1">
                                    <li>กลุ่ม <b>Domain Users</b> เป็นกลุ่มหลัก ไม่สามารถนำออกได้</li>
                                    <li>การเพิ่มกลุ่มอาจใช้เวลา 1-2 นาทีถึงจะเริ่มทำงานในเครื่องลูกข่าย</li>
                                </ul>
                            </div>
                        </div>
                    </div>

                </div>
            </div>

        </div>
    </div>

    <script>
        // ฟังก์ชันเรียก Popup ยืนยัน
        function confirmRemoveGroup(groupName, formId) {
            Swal.fire({
                title: 'ยืนยันการลบ?',
                text: `ต้องการลบ User ออกจากกลุ่ม "${groupName}" ใช่หรือไม่?`,
                icon: 'warning',
                showCancelButton: true,
                confirmButtonColor: '#ef4444', // สีแดง
                cancelButtonColor: '#94a3b8', // สีเทา
                confirmButtonText: 'ใช่, ลบเลย!',
                cancelButtonText: 'ยกเลิก',
                reverseButtons: true,
                focusCancel: true
            }).then((result) => {
                if (result.isConfirmed) {
                    // ถ้ากดยืนยัน ให้ส่งฟอร์ม (Submit)
                    document.getElementById(formId).submit();
                }
            });
        }
    </script>

    <% if (typeof error !== 'undefined' && error) { %>
        <script>
            Swal.fire({
                icon: 'error',
                title: 'เกิดข้อผิดพลาด',
                text: '<%= error %>'
            });
        </script>
    <% } %>

</body>
</html>